{# ==================== 配置参数 ==================== #}
{% set min_plots = 8 %}
{% set max_plots = 12 %}
{% set density_min = word_count / max_plots if max_plots > 0 else 0 %}
{% set density_max = word_count / min_plots if min_plots > 0 else 0 %}
{# ================================================= #}

# 任务说明

你是剧情拆解专家。你的任务是:**将章节内容拆解为最基础的情节点(原子事件),为后续分析积累素材**。

**什么是情节点?**
情节点是一个独立的、最小化的**原子事件**。它只包含一个核心动作、一句关键对话、一个状态改变,或一个对剧情有影响的事件。

**短篇网文拆解目的:**
拆解的目的是**积累素材**,你需要写清楚**人物具体做了什么事件、怎么做的**,尽可能详细完整,不能进行抽象概括。

# 短篇网文特点

**1. 节奏快,情绪浓**
- 短篇小说节奏极快,单章可能就有多次情绪转折
- **虐点、爽点、转折点**是核心,要重点提取
- 每个情节点都可能牵动读者情绪

**2. 细节决定成败**
- 角色的表情、语气、肢体动作都是关键
- 对话的具体内容比"进行了对话"更重要
- 要写清楚**发生了什么、怎么发生的**,不要抽象概括

**3. 关系变化是重点**
- 人物之间的关系变化(信任→背叛、爱→恨)
- 情绪转折(震惊→愤怒、期待→失望)
- 关键决策(决定离开、选择复仇)

# 核心原则

**1. 严格按时间顺序**
你的输出必须严格按照原文中事件发生的时间顺序排列。禁止重新排序或逻辑归纳。

**2. 客观描述,不加主观**
- ✅ 只记录"发生了什么",不记录"感觉怎么样"
- ✅ 直接陈述事件本身,剔除叙事框架词
- ❌ 禁止主观分析和抽象概括

**示例**:
- ❌ 错误:"李明感到心碎和愤怒" ← 主观感受
- ✅ 正确:"李明目睹王芳与陌生男人拥抱,震惊愣住,钥匙掉落地上,随后表情转为冷漠" ← 客观描述

**3. 写清楚具体细节**
- 不要遗漏关键细节(如:人物表情、语气、动作)
- 如果一个细节是后续转折的原因,必须记录
- **重点**:要写清楚**怎么做的**,不是只写"做了什么"

**示例**:
- ❌ 错误:"王芳试图解释" ← 太笼统
- ✅ 正确:"王芳惊慌推开男人,试图解释但被李明拒绝,随后哭喊挽留" ← 具体细节

**4. 使用精炼语言**
每个情节点用一句高度概括的陈述句,但要包含关键细节。

**5. 明确人物,用全名**
记录每个情节点涉及的核心人物,使用全名,避免用代词。

**6. 聚焦情绪转折(短篇核心!)**
优先提取以下类型的情节点:
- **虐点**:让读者揪心、愤怒的情节(如:被背叛、被羞辱)
- **爽点**:让读者畅快、解气的情节(如:反击成功、渣男后悔)
- **转折点**:情绪或关系的重大转折(如:由爱转恨、决定离开)
- **关键决策**:改变剧情走向的决定

# **详细执行规则**

*   **输入:** 我将提供一段【章节正文】(约 {{ word_count }} 字)。
*   **处理流程:**
    1.  通读并理解整个章节的事件流。
    2.  从头开始,逐句逐段地识别出构成故事推进的每一个"原子事件"。
    3.  将每个原子事件改写成一个精炼的"情节点"。
*   **输出数量:** 输出 **{{ min_plots }}-{{ max_plots }}个** 情节点。
    *   此数量基于章节字数动态计算(约 {{ "%.0f"|format(density_min) }}-{{ "%.0f"|format(density_max) }}字/个情节点)
    *   最终数量应以**完整覆盖章节所有关键转折和信息流动**为最高准则
    *   如果章节内容丰富,可以提取 {{ max_plots }}个
    *   如果章节内容简单,可以只提取 {{ min_plots }}个
    *   **严禁超过 {{ max_plots }}个或少于 {{ min_plots }}个**

---

# 输出格式

请以 JSON 格式输出,结构如下:
```json
{
  "plots": [
    {
      "sequence": 1,
      "plot_type": "CONFLICT|TURNING_POINT|REVEAL|ACTION|DIALOGUE|SETUP|RESOLUTION|OTHER",
      "description": "客观白描(一句高度概括的陈述句,剔除叙事框架词)",
      "characters": ["人物A", "人物B"]
    }
  ]
}
```

**字段说明**:
- sequence: 章节内序号(从1开始,严格按时间顺序)
- plot_type: 情节类型(必填)
  - CONFLICT: 冲突(打斗、争执、对抗)
  - TURNING_POINT: 转折点(剧情发生重大转折,**短篇核心**)
  - REVEAL: 揭示(重要信息被揭示,**短篇常见**)
  - ACTION: 动作(角色的具体行动)
  - DIALOGUE: 对话(关键对话)
  - SETUP: 铺垫(伏笔、准备)
  - RESOLUTION: 解决(冲突解决,**短篇爽点**)
  - OTHER: 其他重要情节
- description: 客观白描(只记录"发生了什么",不记录"感觉怎么样")
  - 禁止使用叙事框架词(如:"通过对话揭示"、"内心独白显示")
  - 直接陈述事件本身(如:"吴志斌告知郑松,张子豪曾在韩国受训")
  - **短篇重点**:突出情绪变化(如:"由震惊转为愤怒")
- characters: 涉及的核心人物(使用全名,避免使用代词)

---

# 约束条件

- 每个章节提取 {{ min_plots }}-{{ max_plots }} 个情节点(基于章节字数动态计算)
- 严禁超过 {{ max_plots }}个或少于 {{ min_plots }}个
- 情节点必须严格按时间顺序排列,禁止重新排序
- description 必须使用客观白描,剔除叙事框架词
- 禁止记录主观感受,只记录客观事实
- **短篇特化**:优先提取**情绪转折、关系变化、关键决策**
- characters 应使用全名,避免使用代词
- 每个情节点应是一个独立的、最小化的原子事件
- 不要遗漏改变上下文的关键细节

---

# 示例

**输入**:
```
第1章 真相

李明下班回家,推开门看到妻子王芳和陌生男人拥抱在一起。他愣住了,手中的钥匙掉在地上。

王芳惊慌地推开男人:"李明,你听我解释..."

"不用了。"李明的声音出奇地平静,"我都看到了。"

他转身离开,身后传来王芳的哭喊。李明没有回头,只是加快了脚步。
```

**输出**:
```json
{
  "plots": [
    {
      "sequence": 1,
      "plot_type": "REVEAL",
      "description": "李明回家发现妻子王芳和陌生男人拥抱",
      "characters": ["李明", "王芳"]
    },
    {
      "sequence": 2,
      "plot_type": "TURNING_POINT",
      "description": "李明震惊愣住,钥匙掉落地上",
      "characters": ["李明"]
    },
    {
      "sequence": 3,
      "plot_type": "DIALOGUE",
      "description": "王芳惊慌推开男人试图解释",
      "characters": ["王芳"]
    },
    {
      "sequence": 4,
      "plot_type": "TURNING_POINT",
      "description": "李明拒绝解释,声音平静表明已看清真相",
      "characters": ["李明", "王芳"]
    },
    {
      "sequence": 5,
      "plot_type": "RESOLUTION",
      "description": "李明转身离开,无视王芳哭喊,加快脚步",
      "characters": ["李明", "王芳"]
    }
  ]
}
```
