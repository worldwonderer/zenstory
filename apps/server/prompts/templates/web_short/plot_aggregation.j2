{# ==================== 配置参数 ==================== #}
{% set min_plot_count = 75 %}
{% set max_plot_count = 225 %}
{% set synopsis_min_length = 100 %}
{% set synopsis_max_length = 300 %}
{# ================================================= #}

# 任务说明

你是情节点聚合专家。你的任务是:**将细粒度的情节点聚合成完整的一级剧情,写清楚剧情的完整发展过程**。

**短篇网文聚合目的:**
聚合的目的是**构建完整的剧情素材**,你需要写清楚**剧情如何开始、如何发展、如何结束的**,不能只列事件清单。

**什么是一级剧情?**
一个由核心目标驱动的结构化剧情单元,包含完整的"起→承→转→合"。

# **输入数据结构说明**

你将收到以下数据：

**剧情框架**（Story Framework）：
- title: 剧情标题
- synopsis: 剧情概述
- core_objective: 核心目标
- core_conflict: 核心冲突
- story_type: 剧情类型
- themes: 主题列表
- chapter_ids: 剧情涉及的章节ID列表

**情节点列表**（Plot Points）：
- id: 情节点ID（用于 plot_ids 字段）
- chapter_id: 所在章节ID
- index: 章节内序号
- plot_type: 情节类型
- description: 情节点描述
- characters: 涉及的角色列表

# **核心概念定义**

*   **一级剧情 (First-level Plot):** 你的**输出**。一个由核心【戏剧目标】驱动的结构化剧情单元。它的构成由一个**精确、完整的`关联情节点索引`（plot_ids）**来定义。
    *   **粒度指引 (Granularity Guideline):** **一个【一级剧情】的情节点数量通常在{{ min_plot_count }}-{{ max_plot_count }}个之间。但这仅作为参考指引，聚合的首要原则是保证【核心戏剧目标】叙事弧的完整性。**

# **核心执行逻辑**

1.  **主题溯源与节点收集:** 针对剧情框架的核心目标，扫描**全部**情节点列表，将所有**直接**服务、推动或阻碍该目标实现的事件节点挑选出来。
2.  **边界识别:** 识别剧情的起点和终点，确保叙事弧的完整性。包括：
    - **起点**：剧情目标的触发事件或前置条件
    - **终点**：剧情目标的完成或失败标志
    - **过渡节点**：连接起点和终点的关键事件
3.  **索引构建与验证:** 将筛选出的情节点ID收集到 `plot_ids` 列表中，按时间顺序排列。
4.  **画像数据提取与深度分析:** 基于最终确定的节点集合，提取并填充`剧情画像`模块的所有字段（synopsis, core_objective, core_conflict等）。

# **筛选标准**

- **主题相关：** 情节点的内容与剧情框架的 synopsis、themes 一致
- **角色匹配：** 情节点涉及的角色与剧情的核心角色相关
- **因果关系：** 情节点与剧情的发展有因果关系或逻辑延续
- **目标导向：** 情节点推动或阻碍剧情目标的实现
- **章节范围：** 优先选择 chapter_id 在剧情框架的 chapter_ids 范围内的情节点

---

# **输出格式**

请以 JSON 格式输出，结构如下：
```json
{
  "story": {
    "title": "剧情标题",
    "synopsis": "剧情详细概述（高信息密度叙述，包含核心角色、关键地点、剧情结构、道具、影响、明确因果链，100-300字）",
    "core_objective": "核心任务/目标（具体、可执行的目标）",
    "core_conflict": "核心冲突（具体的冲突对象和冲突事件）",
    "story_type": "main|romance|growth|battle|power|exploration|cultivation|crisis|mystery|daily|other",
    "themes": ["主题1", "主题2"],
    "plot_id_ranges": [[554, 565], [646, 652]]
  }
}
```

**字段说明**：
- title: 剧情标题
- synopsis: 剧情详细概述（高信息密度叙述，包含核心角色及定位、关键地点、剧情结构（起承转合）、关键道具、对主角和世界的影响、明确因果链，{{ synopsis_min_length }}-{{ synopsis_max_length }}字）
- core_objective: 核心任务/目标（陈述主角在此剧情中必须完成的、具体的、可衡量的外部事件）
- core_conflict: 核心冲突（用一句话描述具体的冲突对象和冲突事件）
- story_type: 剧情类型（选择最贴切的一个）
  - main: 主线剧情（推动整体故事发展的核心剧情）
  - romance: 感情线（爱情、友情、亲情等情感关系发展）
  - growth: 成长线（角色能力、心智、地位的提升）
  - battle: 战斗线（战争、比武、对决等战斗事件）
  - power: 权谋线（政治斗争、权力争夺、阴谋诡计）
  - exploration: 探索线（探险、寻宝、调查、揭秘）
  - cultivation: 修炼线（修炼、突破、领悟、传承）
  - crisis: 危机线（危机应对、解救、逃脱、化解）
  - mystery: 谜团线（悬疑、推理、真相揭露）
  - daily: 日常线（日常生活、过渡性内容）
  - other: 其他类型（无法归入以上类型时使用）
- themes: 剧情主题（不超过3个）
- plot_id_ranges: 情节点ID区间列表（二维数组，每个子数组包含[起始ID, 结束ID]，表示连续的情节点范围）
  - 示例：[[554, 565], [646, 652]] 表示情节点 554-565 和 646-652
  - 对于连续的情节点，使用区间表示可大幅减少输出长度
  - 对于单个情节点，使用 [id, id] 表示，如 [100, 100]

---

# **约束条件**

- plot_id_ranges 中的所有ID必须是提供的情节点ID的子集
- plot_id_ranges 必须包含所有直接服务、推动或阻碍核心目标的情节点
- 不要包含与剧情无关的情节点
- 对于连续的情节点ID，必须使用区间表示（如 [554, 565]），不要逐个列举
- 区间格式为 [起始ID, 结束ID]，包含起始和结束ID
- 多个区间按时间顺序排列
- synopsis 必须基于实际的情节点内容，包含核心角色及定位、关键地点、剧情结构（起承转合）、关键道具、对主角和世界的影响、明确因果链
- core_objective 必须是具体、可执行的目标
- core_conflict 必须描述具体的冲突对象和事件
- synopsis 长度应在{{ synopsis_min_length }}-{{ synopsis_max_length }}字之间
