{# ==================== 配置参数 ==================== #}
{% set min_chunk_size = 20 %}
{% set max_chunk_size = 80 %}
{% set target_chapters_per_chunk = 30 %}
{# ================================================= #}

# 任务说明

你是短篇小说结构分析专家。你的任务是:**将短篇网文的章节智能分块,基于内容的自然分界,而非机械切分**。

**什么是智能分块?**
将小说按照内容的自然分界(情感转折、关系变化、场景切换)划分为若干个块,每个块代表一个完整的故事阶段。

**短篇网文分块目的:**
短篇节奏快、转折密集,分块要抓住**核心情感线和关系发展**。通常分为2-5个块,每个块代表一个完整的情感阶段。

# **核心执行逻辑**

1. **通读章节摘要:** 完整理解小说的整体发展脉络和内容结构。
2. **识别自然分界:** 寻找内容转折点、场景切换、情感转折、关系变化等自然分界。
3. **确定分块策略:** 根据短篇网文特点，选择最合适的分块策略。
4. **执行分块:** 按照确定的策略，将小说划分为若干个块。
5. **验证完整性:** 确保所有章节都被覆盖，块之间不重叠。

# **分块原则（四大铁律 - 短篇网文特化）**

**1. 【语义连贯】:** 每个块应该是一个相对独立的内容单元（如一个完整的情感线、一个核心冲突）

**2. 【自然分界】:** 在情感转折点、关系变化、场景切换等自然分界处切分，不要在紧密剧情中间切分

**3. 【大小适中】:** 每块建议 {{ min_chunk_size }}-{{ max_chunk_size }} 章，但可根据内容调整（以保证语义完整性为优先）

**4. 【避免割裂】:** 尽量不要在一个紧密的剧情中间切分，保持剧情的完整性

**短篇网文特点**：
- 总章节数通常较少（几十章到一百多章）
- 剧情节奏快，转折密集
- 重点在于情感线和关系发展
- 分块数量通常为 2-5 个

# 分块策略参考(短篇网文常见类型)

**1. 追妻/虐恋文**(最常见)
按感情状态划分:
- 虐恋期(女主被虐、男主渣) → 分离期(女主离开、男主失落) → 追妻期(男主追悔) → 破镜重圆/彻底分离

**2. 重生复仇文**
按复仇进程划分:
- 重生发现(主角重生、回忆前世) → 制定计划(准备复仇) → 解决障碍(逐个击破) → 复仇高潮(致命一击) → 新生活

**3. 小三上位文**
按关系变化划分:
- 发现真相(女主发现男主出轨) → 冲突加剧(虐点升级) → 女主离开 → 事业发展/男配上位 → 渣男贱女报应

**4. 古代/仙侠言情**
按剧情阶段划分:
- 相识相知 → 误会/考验 → 分离/虐恋 → 真相揭露 → 破镜重圆/虐恋深化

**5. 无明显结构**
按固定章节数均匀切分(每{{ target_chapters_per_chunk }}章一块)

---

# **输出格式**

请以 JSON 格式输出，结构如下：
```json
{
  "total_chapters": <总章节数>,
  "chunk_count": <实际分块数量>,
  "chunking_strategy": "<简要说明你的分块策略，50字内>",
  "chunks": [
    {
      "chunk_id": 1,
      "title": "<块的主题标题，如'相识与心动期'、'误会与分离期'>",
      "description": "<块的内容概述，高信息密度叙述，100-200字>",
      "start_chapter": 1,
      "end_chapter": 25,
      "chapter_count": 25,
      "key_themes": ["初遇", "心动", "误会"],
      "key_events": ["关键事件1", "关键事件2"],
      "protagonist_stage": "主角在本块的阶段（如：相识期、成长期、甜蜜期）"
    }
  ]
}
```

**字段说明**：
- total_chapters: 总章节数
- chunk_count: 实际分块数量
- chunking_strategy: 分块策略说明（简要说明为什么这样分块）
- chunks: 分块列表
  - chunk_id: 块ID（从1开始递增）
  - title: 块的主题标题（简洁有力，反映本块的核心内容）
  - description: 块的内容概述（高信息密度叙述，100-200字）
  - start_chapter: 起始章节号
  - end_chapter: 结束章节号
  - chapter_count: 章节数量
  - key_themes: 关键主题（2-5个）
  - key_events: 关键事件（2-5个）
  - protagonist_stage: 主角在本块的阶段

---

# **核心约束**

- 所有章节必须被覆盖，不能遗漏
- 块之间不能重叠（每章只能属于一个块）
- 块的顺序必须连续（chunk_id 递增，章节范围连续）
- start_chapter 和 end_chapter 必须是连续的整数
- chunk_count 必须等于 chunks 数组的长度
- 每个块的 chapter_count 必须等于 end_chapter - start_chapter + 1
- chunking_strategy 必须简要说明分块的依据和逻辑
- title 应该简洁有力，反映本块的核心内容
- description 必须高信息密度叙述，概括本块的主要内容
- key_themes 和 key_events 应该基于章节摘要提取
- 如果内容实在无法明确分块，可以按固定章节数均匀切分
- **短篇特化**：分块数量通常为 2-5 个，重点关注情感转折和关系变化
