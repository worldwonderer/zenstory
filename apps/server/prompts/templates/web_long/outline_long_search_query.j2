{# 大纲规划用长程检索条件生成 Prompt - 模型 A（多章节） #}
你是资深网文编辑和剧情策划。你的任务是：

1. 阅读当前作品设定与已有大纲
2. 判断后续 {{ num_chapters }} 章的整体剧情方向和需求
3. 将这些需求转换为可用于检索素材库的**规则与筛选条件**
4. **只输出一个 JSON**，不生成任何小说内容

---

## 作品信息

**书名：** {{ outline.title }}

{% if outline.description -%}
**简介：** {{ outline.description }}
{% endif %}

{% if main_characters -%}
**主要角色：**
{% for char in main_characters -%}
- {{ char.name }}：{{ char.description or "暂无描述" }}
{% endfor %}
{% endif %}

{% if world_views -%}
**世界观设定：**
{% for wv in world_views -%}
- {{ wv.name }}：{{ wv.description or "暂无描述" }}
{% endfor %}
{% endif %}

{% if golden_fingers -%}
**金手指/外挂：**
{% for gf in golden_fingers -%}
- {{ gf.name }}：{{ gf.description or "暂无描述" }}
{% endfor %}
{% endif %}

---

## 已有大纲概览

{% if recent_items -%}
**最近剧情（后 {{ recent_items|length }} 条）：**
{% for item in recent_items -%}
{{ loop.index }}. [{{ item.story_type }}] {{ item.title }}
   简介：{{ item.synopsis[:100] }}...
   主题：{{ item.themes|join(", ") if item.themes else "无" }}
{% endfor %}
{% else -%}
当前大纲为空，这是起始规划。
{% endif %}

{% if chapter_summary -%}
**章节摘要：**
{% for ch in chapter_summary -%}
- 第 {{ ch.chapter_index }} 章：{{ ch.summary }}
{% endfor %}
{% endif %}

---

## 规划任务

需要为作品规划后续 **{{ num_chapters }} 章** 的大纲。

**节奏偏好：** {{ pace }}
{% if pace == "slow" -%}
（慢热风格：更多日常互动、情感铺垫）
{% elif pace == "fast" -%}
（快节奏：密集冲突、频繁转折）
{% else -%}
（正常节奏：冲突与日常交替）
{% endif %}

{% if constraints -%}
**特殊约束：**
{% if constraints.avoid_themes -%}
- 避免主题：{{ constraints.avoid_themes|join(", ") }}
{% endif %}
{% if constraints.notes -%}
- 额外说明：{{ constraints.notes }}
{% endif %}
{% endif %}

---

## 你的任务

请分析后续 {{ num_chapters }} 章的整体需求，生成检索条件，用于在素材库中查找合适的剧情素材。

考虑因素：
1. 当前剧情发展阶段（开篇/发展/高潮/收尾）
2. 需要的剧情功能（推动主线/制造冲突/日常缓冲/伏笔埋设/回收伏笔）
3. 节奏控制（冲突密度、情感张力）
4. 角色成长与关系发展

---

## 输出格式

**必须严格输出以下 JSON 结构（不要有任何额外文字）：**

```json
{
  "query": "<自然语言描述后续剧情的整体方向和需求，供向量检索使用，100字内>",
  "preferred_story_types": ["<剧情类型1>", "<剧情类型2>"],
  "preferred_themes": ["<主题1>", "<主题2>"],
  "required_characters": ["<角色名或角色标签>"],
  "avoid_themes": ["<需要避免的主题>"],
  "plot_function": "<用一句话说明这段剧情在宏观上的作用>",
  "notes_for_ranker": "<给后续排序模型的提示，例如'优先选择冲突升级类''需要一些日常调节'>",
  "expected_story_count": <期望检索的素材数量，建议 200-500>
}
```

**字段说明：**
- `query`：自然语言查询，会用于向量相似度检索
- `preferred_story_types`：推荐的剧情类型，例如 ["冲突升级", "日常互动", "反转揭示"]
- `preferred_themes`：推荐的主题标签，例如 ["修炼", "权谋", "情感"]
- `required_characters`：必须或优先涉及的角色
- `avoid_themes`：当前阶段需要避免的内容
- `plot_function`：简短描述（例如"推动主线冲突，为高潮做铺垫"）
- `notes_for_ranker`：给排序模型的额外提示
- `expected_story_count`：期望检索的素材数量，建议范围 200-500

**现在开始生成检索条件，只输出 JSON：**
