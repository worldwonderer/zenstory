{# 大纲章节规划 Prompt - 模型 B（计划多章节） #}
你是资深网文主编，需要用下列【素材库剧情】来规划作品的后续章节大纲。

**核心约束：**
1. **只能使用提供的素材库 Story**，不允许编造新剧情
2. **必须输出结构化 JSON**，不写任何散文式小说内容
3. 考虑节奏合理（冲突与日常交替）、人物成长、伏笔埋设与回收

---

## 作品信息

**书名：** {{ outline.title }}

{% if outline.description -%}
**简介：** {{ outline.description }}
{% endif %}

{% if main_characters -%}
**主要角色：**
{% for char in main_characters -%}
- {{ char.name }}：{{ char.description or "暂无描述" }}
{% endfor %}
{% endif %}

{% if world_views -%}
**世界观设定：**
{% for wv in world_views -%}
- {{ wv.name }}：{{ wv.description or "暂无描述" }}
{% endfor %}
{% endif %}

{% if golden_fingers -%}
**金手指/外挂：**
{% for gf in golden_fingers -%}
- {{ gf.name }}：{{ gf.description or "暂无描述" }}
{% endfor %}
{% endif %}

---

## 已有大纲概览

{% if recent_items -%}
**最近剧情（后 {{ recent_items|length }} 条）：**
{% for item in recent_items -%}
{{ loop.index }}. [{{ item.story_type }}] {{ item.title }}
   简介：{{ item.synopsis[:100] }}...
   主题：{{ item.themes|join(", ") if item.themes else "无" }}
{% endfor %}
{% else -%}
当前大纲为空，这是起始规划。
{% endif %}

{% if chapter_summary -%}
**章节摘要：**
{% for ch in chapter_summary -%}
- 第 {{ ch.chapter_index }} 章：{{ ch.summary }}
{% endfor %}
{% endif %}

---

## 规划任务

**目标：** 规划后续 {{ num_chapters }} 章大纲

**每章节数范围：** {{ min_items_per_chapter }}-{{ max_items_per_chapter }} 节

**节奏偏好：** {{ pace }}
{% if pace == "slow" -%}
（慢热风格：更多日常互动、情感铺垫、细腻心理描写）
{% elif pace == "fast" -%}
（快节奏：密集冲突、频繁转折、高强度爽点）
{% else -%}
（正常节奏：冲突与日常交替，张弛有度）
{% endif %}

{% if constraints -%}
**特殊约束：**
{% if constraints.avoid_themes -%}
- 避免主题：{{ constraints.avoid_themes|join(", ") }}
{% endif %}
{% if constraints.notes -%}
- 额外说明：{{ constraints.notes }}
{% endif %}
{% endif %}

{% if avoid_used_stories -%}
**约束：** 优先使用未在当前大纲中使用过的 Story
{% endif %}

---

## 可用素材库 Story（候选集）

以下是检索到的 {{ candidates|length }} 个候选剧情素材，你只能从中选择：

{% for story in candidates -%}
**[{{ loop.index }}] Story ID: {{ story.id }}**
- 标题：{{ story.title }}
- 简介：{{ story.synopsis }}
- 类型：{{ story.story_type }}
- 主题：{{ story.themes|join(", ") if story.themes else "无" }}
{% if story.already_used -%}
- ⚠️ 已在当前大纲使用过
{% endif %}

{% endfor %}

---

## 输出格式要求

**必须严格输出以下 JSON 结构（不要有任何额外文字）：**

```json
{
  "chapters": [
    {
      "chapter_index": <相对索引，从 {{ from_chapter_index }} 开始>,
      "chapter_title_suggestion": "<建议的章节标题>",
      "chapter_goal": "<本章的剧情使命，1-2 句话>",
      "items": [
        {
          "story_id": <必须是上述候选集中的 Story ID>,
          "role": "<这个 Story 在本章中的功能，例如'冲突爆点''日常互动''伏笔埋设'>"
        }
      ]
    }
  ],
  "reasoning": "<简要说明整体规划思路，2-3 句话>"
}
```

**关键要求：**
1. `story_id` 必须是上述候选集中的真实 ID，不允许编造
2. `chapter_index` 从 {{ from_chapter_index }} 开始递增
3. 每章的 `items` 数量在 {{ min_items_per_chapter }}-{{ max_items_per_chapter }} 之间
4. `role` 字段简短明了，不超过 20 字
5. 整体 `chapters` 数组长度为 {{ num_chapters }}

**现在开始规划，只输出 JSON：**
