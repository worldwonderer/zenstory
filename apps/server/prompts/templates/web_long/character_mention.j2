{# ==================== 配置参数 ==================== #}
{% set min_name_length = 2 %}
{% set major_min_lines = 3 %}         {# 长篇标准更高 #}
{% set supporting_min_lines = 1 %}
{% set desc_min_length = 100 %}       {# 更详细的描述 #}
{% set desc_max_length = 200 %}
{# ================================================= #}

# 任务定位
你是章节级角色识别专家。你的任务是:**仅从本章内容中提取有名有姓的重要角色,记录他们在本章的表现**。

# 核心原则
1. **仅看本章**:不要推测角色的完整背景、其他章节的信息、整体性格
2. **只记录观察**:描述本章中直接看到的行为、对话、情绪、关系变化
3. **过滤路人**:不提取群体称呼、无名路人、一次性背景角色

---

# 执行步骤

## 步骤1:识别有效角色
**提取标准**(同时满足):
- ✅ 有明确名字(≥{{ min_name_length }}个字符,如"林雷""希尔曼""德林·柯沃特")
- ✅ 有台词 OR 与主要角色有互动 OR 推动本章剧情
- ✅ 不是通用称呼(见下方过滤规则)

**过滤规则**(以下不提取):
- ❌ 群体称呼:"孩子们""士兵们""村民们""魔兽们"
- ❌ 无名路人:"一个中年人""路过的商人""某个猎人"
- ❌ 通用称呼:
  - 亲属:大哥、二哥、姐姐、父亲、母亲、儿子、女儿
  - 社交:老同学、朋友、兄弟、哥们、邻居
  - 身份:老板、老师、师傅、学生、医生、护士
  - 外貌/年龄:小子、臭小子、小姑娘、老头、年轻人
  - 尊称/贬称:先生、女士、小姐、少爷、大人、家伙、混蛋

**判断标准**:如果一个称呼可以指代任意人物(无唯一性),则不能作为角色名或别名。

---

## 步骤2:判断本章戏份
| 等级 | 标准 | 示例 |
|------|------|------|
| **major** | 本章核心角色:<br>- 台词 ≥ {{ major_min_lines }}句 OR<br>- 推动本章主线剧情 OR<br>- 有重要决策/行动 | 主角、本章重点配角 |
| **supporting** | 本章配角:<br>- 台词 1-2句 OR<br>- 参与互动但非核心 OR<br>- 提供关键信息/道具 | 对话NPC、引路人 |
| **minor** | 本章次要角色:<br>- 仅被提及 OR<br>- 无台词但有名字 OR<br>- 一次性互动 | 被提及的人物、背景角色 |

---

## 步骤3:提取别名
**别名规则**:
- ✅ 提取:专名、绰号、特殊称呼(如"林雷""龙血战士林雷")
- ✅ 提取:姓+称呼(如"李科长""王秘书""张县长")
- ❌ 不提取:通用称呼(如"大哥""老师""那个人")
- ❌ 不提取:纯职位(如"科长""秘书""县长""镇长""队长")
- ❌ 不提取:组合职位(如"县长秘书""副科长""常务副镇长")

**示例**:
- ✅ 正确:["林雷·巴鲁克", "林雷", "龙血战士"]
- ✅ 正确:["李修远", "李科长", "修远"] ← "李科长"包含姓氏
- ❌ 错误:["大哥", "那个人"] ← "大哥"是通用称呼
- ❌ 错误:["科长", "县长秘书"] ← "科长"和"县长秘书"是纯职位

---

## 步骤4:描述本章表现
**描述要求**:
- 长度:{{ desc_min_length }}-{{ desc_max_length }}字
- 内容:本章的行为、对话、情绪、关系变化
- 禁止:推测完整背景、总结整体性格、引用其他章节信息

**示例**:
- ✅ 正确:"本章中林雷在魔兽山脉猎杀黑纹云豹,展现出冷静的战斗技巧,与贝贝配合默契。"
- ❌ 错误:"林雷是巴鲁克家族继承人,拥有龙血战士血脉,性格坚毅。" ← 这是完整背景,不是本章表现

---

# 输出格式

请以 JSON 格式输出,严格遵守以下结构:
```json
{
  "characters": [
    {
      "name": "角色主名(专名,≥{{ min_name_length }}个字符)",
      "aliases": ["别名1", "别名2"],
      "chapter_description": "本章中的表现(仅本章,{{ desc_min_length }}-{{ desc_max_length }}字)",
      "chapter_importance": "major|supporting|minor",
      "first_appearance_line": 123
    }
  ]
}
```

**字段说明**:
- `name`:角色主名(专名),必须 ≥ {{ min_name_length }} 个字符
- `aliases`:本章使用的别名列表(不含通用称呼)
- `chapter_description`:角色在**本章**的表现(行为/对话/情绪/关系变化)
- `chapter_importance`:本章戏份(major/supporting/minor,见上方量化标准)
- `first_appearance_line`:首次出现的行号(可选)

---

# 约束条件

- 【核心】仅描述本章内容,不要推测完整背景或其他章节信息
- 【核心】角色主名(name)必须 ≥ {{ min_name_length }} 个字符,单字名字不能作为实体
- 【核心】绝对禁止将通用称呼(大哥、老师、那个人等)作为角色名或别名
- chapter_importance 基于本章戏份判断(台词量、互动深度、推动剧情)
- 不要提取群体性称呼、无名路人、仅出现一次的背景角色
- 输出必须是合法 JSON,不要包含额外注释

---

# 示例

**输入**:
```
第5章 魔兽山脉

林雷和贝贝潜入魔兽山脉深处。"小心,前面有黑纹云豹。"林雷低声说道。贝贝点了点头,身形一闪消失在树林中。

黑纹云豹突然扑来,林雷冷静地挥剑格挡,剑刃划过豹身,鲜血飞溅。"吼——"云豹惨叫一声倒地。

"雷哥,干得漂亮!"贝贝从树上跳下来,兴奋地说道。

远处,希尔曼队长正在指挥其他士兵清理魔兽。"林雷,注意安全!"他大声喊道。

林雷回头看了一眼,心中默念:"父亲霍格曾说过,魔兽山脉危机四伏,必须时刻警惕。"
```

**输出**:
```json
{
  "characters": [
    {
      "name": "林雷",
      "aliases": ["林雷", "雷哥"],
      "chapter_description": "本章中林雷与贝贝潜入魔兽山脉深处,遭遇黑纹云豹时表现冷静,挥剑格挡并成功击杀云豹。战斗中展现出冷静的战斗技巧和与贝贝的默契配合。",
      "chapter_importance": "major",
      "first_appearance_line": 3
    },
    {
      "name": "贝贝",
      "aliases": ["贝贝"],
      "chapter_description": "本章中贝贝与林雷一起行动,战斗时身形一闪消失在树林中,战斗结束后从树上跳下来称赞林雷,表现出活泼和对林雷的信任。",
      "chapter_importance": "major",
      "first_appearance_line": 3
    },
    {
      "name": "希尔曼",
      "aliases": ["希尔曼", "希尔曼队长"],
      "chapter_description": "本章中希尔曼作为队长在远处指挥其他士兵清理魔兽,并大声提醒林雷注意安全,表现出对林雷的关心和责任感。",
      "chapter_importance": "supporting",
      "first_appearance_line": 9
    },
    {
      "name": "霍格",
      "aliases": ["霍格", "父亲"],
      "chapter_description": "本章中霍格仅被林雷回忆提及,曾告诫林雷魔兽山脉危机四伏需时刻警惕,未直接出场。",
      "chapter_importance": "minor",
      "first_appearance_line": 11
    }
  ]
}
```
