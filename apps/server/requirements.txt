# FastAPI and dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlmodel>=0.0.14
python-multipart>=0.0.6
pydantic-settings>=2.0.0

# OpenAI API client
openai>=1.3.0

# Anthropic SDK for Claude API
anthropic>=0.40.0

# Prefect for workflow orchestration
prefect>=3.4.0

# Zhipu AI (embeddings)
# Provides `from zai import ZhipuAiClient`
zai-sdk>=0.2.0

# Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
authlib>=1.2.0
httpx>=0.24.0

# Email validation (required by Pydantic EmailStr)
email-validator>=2.1.0

# Vector computation
numpy>=1.24.0

# File encoding detection
chardet>=5.0.0

# Chinese number conversion (中文数字转阿拉伯数字)
cn2an>=0.5.0

# Neo4j graph database (optional, for knowledge graph)
neo4j>=5.0.0

# JSON repair for tool calling robustness
json-repair>=0.40.0

# Database
aiosqlite>=0.19.0
psycopg2-binary>=2.9.9  # PostgreSQL support (sync)
psycopg[binary]>=3.1.0  # PostgreSQL psycopg3 driver
asyncpg>=0.29.0  # Async PostgreSQL driver

# LlamaIndex for RAG and retrieval
llama-index-core>=0.10.0
llama-index-embeddings-openai>=0.1.0
llama-index-vector-stores-chroma>=0.1.0

# Chroma vector database
chromadb>=0.4.0
posthog>=2.4.0,<6.0.0  # chromadb telemetry compatibility

# Database migrations (recommended for production)
alembic>=1.12.0

# Redis for caching and verification codes
redis>=5.0.0

# Email service
resend>=0.8.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-xdist>=3.5.0
responses>=0.23.0

# Linting (for pre-commit hooks)
ruff>=0.4.0
pylint>=3.0.0
